import { createStore } from 'vuex';

export default createStore({
  state: {
    // state properties
  },
  mutations: {
    // mutation methods
  },
  actions: {
    // action methods
  },
  modules: {
    // sub-modules
  }
});